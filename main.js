import { dupa, createData } from "./modules/module1.js";

const submitBtn = document.getElementById("submitBtn");

submitBtn.addEventListener("submit", (event) => {
  event.preventDefault();
  //createData();
});
